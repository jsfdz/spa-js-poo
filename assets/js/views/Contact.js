export function Contact() {
    const
        container = document.createElement('div')

    container.classList.add('container')
    container.innerHTML = `<h1>Contact Page</h1>`
    return container
}